# CampusRent frontend production example
# If frontend is served over HTTPS (CloudFront/custom domain), your API and socket URLs
# must also be HTTPS or browser requests will be blocked as mixed content.

VITE_API_URL=https://YOUR_BACKEND_PUBLIC_HOST/api
VITE_SOCKET_URL=https://YOUR_BACKEND_PUBLIC_HOST
